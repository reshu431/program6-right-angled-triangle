#include<stdio.h>
int main()
{
      int n,i,j;
      printf("enter the row of the matrix:");
      scanf("%d",&n);
      for(i=1;i<=n;i++){
          for(j=1;j<=1;j++){
              printf("%d",i);
      }
      printf("\n");
      }
      return 0;
}
